# SIMS AI backend — runtime dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic-settings>=2.0
PyJWT>=2.8
argon2-cffi>=23.1

# Production database driver (tests/dev fall back to SQLite)
psycopg2-binary>=2.9; extra == "postgres"
